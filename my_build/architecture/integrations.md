# Integrations

## ChatGPT Web and Chrome

The Windows bridge host owns the signed-in browser profile. Cookies, signed URLs, and Windows paths remain on the host.

## SSH bridge

Case3 uses a reverse tunnel from Windows to cloud Linux. Use the configured fixed bridge token and `127.0.0.1`; do not start duplicate hosts or tunnels.

## GitHub

- Fork: `DK625/oracle`.
- Upstream: `steipete/oracle`.
- Upstream feature PR: https://github.com/steipete/oracle/pull/277

## AI agents

Codex/Claude start with `AGENTS.md` and `my_build/INDEX.md`, then load only task-routed memory.
