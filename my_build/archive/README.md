# Archive

Store deprecated but still useful context here.

Rules:

- Move stale/deprecated docs here instead of deleting immediately.
- Mark why the context is archived.
- Do not archive raw logs, runtime dumps, binary data, or private secrets.
- Do not rely on archived docs unless a task explicitly asks for historical context.
